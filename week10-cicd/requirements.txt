# warrantyAI — Week 10: CI/CD + Prompt Regression Tests
#
# Runtime + test dependencies
# Same core stack as week8/9; boto3 is pre-installed in the Lambda base image.

# LangGraph pipeline
langgraph>=0.2.0
langchain-core>=0.2.0

# AWS SDK
boto3>=1.34.0

# Test runner
pytest>=8.0.0
pytest-xdist>=3.5.0     # parallel test execution: pytest -n auto
